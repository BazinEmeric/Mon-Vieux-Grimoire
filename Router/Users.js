const express = require("express");
const router = express.Router();
const CrtlUser = require("../Controllers/Users");

/*router.post("/signup", CrtlUser.createUsers);
router.post("/login", CrtlUser.loginUsers);*/

module.exports = router;
